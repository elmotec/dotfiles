# roots
# we need UNC path for compatibility with scheduled tasks
root = \\synology\homes\{{ .chezmoi.username }}\backup
root = C:\Users\{{ .chezmoi.username }}

# Unison preferences file
include common

fastcheck = true
perms = 0
sshargs = -C
